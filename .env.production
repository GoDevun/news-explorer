# Used by `npm run build` (and therefore `npm run deploy`).
# Must point at the deployed API, because GitHub Pages cannot run one.
VITE_API_BASE_URL=https://stock-news-sentiment-api-v6pc.onrender.com
